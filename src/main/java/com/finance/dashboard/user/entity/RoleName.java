package com.finance.dashboard.user.entity;

/**
 * Human-readable role names.
 * Corresponding to the records in the {@code roles} table.
 */
public enum RoleName {
    ADMIN,
    ANALYST,
    VIEWER
}
